import React from "react";
import { Container, Typography } from "@mui/material";

const User = () => {
  return (
    <Container>
      <Typography variant="h4">👤 User Profile</Typography>
      <Typography>User profile management will be added soon!</Typography>
    </Container>
  );
};

export default User;
